apiVersion: v1
kind: Pod
metadata:
  name: test-pod-001
  namespace: default
spec:
  containers:
  - name: app
    image: /tmp/sirah-unikernels/test-kernel.img
    resources:
      limits:
        memory: "256Mi"
        cpu: "1"
